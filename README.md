# CPU-Emulator---C
A lightweight virtual machine implementation written in C (C99). This project  simulates a simplified stack-based processor architecture featuring a custom  instruction set, dynamic memory management, and a complete fetch-decode-execute  cycle.
